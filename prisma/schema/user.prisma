model User {
  id        Int      @id @default(autoincrement())
  token     String
  email     String   @unique
  password  String
  firstName String?
  lastName  String?
  options   Options?
  logs      Logs[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("users")
}
