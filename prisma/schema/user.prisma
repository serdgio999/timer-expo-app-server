model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  password  String
  name      String?
  options   Options?
  logs      Logs?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("users")
}
